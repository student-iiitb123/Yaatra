<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top border-bottom py-3">
  <div class="container-fluid px-4 d-flex align-items-center justify-content-between">

    <!-- Left side: Brand + Explore -->
    <div class="d-flex align-items-center">
      <a class="navbar-brand d-flex align-items-center me-4" href="/">
        <i class="fa-solid fa-compass me-2 text-primary"></i>
        <span class="fw-bold fs-4 text-primary">Yaatra</span>
      </a>
      <ul class="navbar-nav flex-row">
        <li class="nav-item">
          <a class="nav-link text-dark fw-semibold px-3" href="/listings">Explore</a>
        </li>
      </ul>
    </div>

    <!-- Center: Search bar -->
    <div class="mx-auto d-flex" style="max-width: 450px;">
      <input id="searchInput" class="form-control me-2 rounded-pill border-primary" type="search" placeholder="Search country" />
      <button id="searchBtn" class="btn btn-primary rounded-pill d-flex align-items-center px-4">
        <i class="fa-solid fa-magnifying-glass me-2"></i> Search
      </button>
    </div>

    <!-- Right side: Actions -->
    <div class="d-flex align-items-center">
      <ul class="navbar-nav flex-row align-items-center">
        <li class="nav-item me-3">
          <a class="nav-link text-dark fw-semibold px-3" href="/listings/new">New Listing</a>
        </li>
        <li class="nav-item me-3">
          <a class="nav-link text-dark fw-semibold px-3" href="/signup">Sign up</a>
        </li>
        <li class="nav-item me-3">
          <a class="nav-link text-dark fw-semibold px-3" href="/login">Log in</a>
        </li>
      </ul>
    </div>

  </div>
</nav>

<script>
  document.getElementById("searchBtn").addEventListener("click", async (e) => {
    e.preventDefault();
    const country = document.getElementById("searchInput").value;
    if (!country) return alert("Please enter a country!");
    const res = await fetch(`/api/hotels/search?country=${country}`);
    const data = await res.json();
    console.log(data);
  });
</script>
